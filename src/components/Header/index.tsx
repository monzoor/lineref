import { FC } from 'react';

const Header: FC = () => {
  return (
    <div className="w-full h-14 shadow-md bg-primary justify-center flex items-center text-white text-2xl">
      Car booking center
    </div>
  );
};

export default Header;
